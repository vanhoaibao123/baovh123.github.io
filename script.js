function addToCart(productId) {
    // Xử lý logic thêm sản phẩm vào giỏ hàng
    console.log("Sản phẩm " + productId + " đã được thêm vào giỏ hàng.");
    function checkout(productId) {
        // Xử lý logic thanh toán sản phẩm
        console.log("Đã thanh toán sản phẩm " + productId);
    }
    
}
